% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering.R
\name{filter_pathway_by_tsne}
\alias{filter_pathway_by_tsne}
\title{Filter Pathways Based on t-SNE and Clustering to Detect Antagonist Patterns}
\usage{
filter_pathway_by_tsne(
  long_df,
  dose_col = "Dose",
  expression_col = "counts",
  perplexity = 30,
  theta = 0.5,
  min_silhouette = 0.2
)
}
\arguments{
\item{long_df}{Dataframe containing the long format transcriptomic data.}

\item{dose_col}{Character string specifying the name of the dose column in `long_df`. Default is "Dose".}

\item{expression_col}{Character string specifying the name of the expression column in `long_df`. Default is "counts".}

\item{perplexity}{Numeric value specifying the perplexity parameter for t-SNE. Default is 30.}

\item{theta}{Numeric value specifying the theta parameter for t-SNE. Default is 0.5.}

\item{min_silhouette}{Numeric value specifying the minimum silhouette score threshold for filtering pathways. Default is 0.2.}
}
\value{
Logical value: TRUE if the pathway should be kept (no antagonist pattern detected and good clustering), FALSE otherwise.
}
\description{
This function filters pathways in transcriptomic data by performing t-SNE and clustering.
It determines whether to keep or skip a pathway based on silhouette scores of the clusters,
and by checking for antagonist patterns within the clusters.
}
\examples{
\dontrun{
  # Assuming 'long_df' is available and transform_smooth_values function is defined
  should_keep_pathway <- filter_pathway_by_tsne(long_df, dose_col = "Dose", expression_col = "counts", perplexity = 30, theta = 0.5, min_silhouette = 0.2)
  if (should_keep_pathway) {
    # Proceed with further analysis
  }
}
}
